********************************************************************************
*
*
*
*
*
*
*
********************************************************************************

if(jQuery) {
 (function(jq) {
     jq.extend(
         jq.expr[ ":" ],
         {
             appbar : (
                 "jQuery().attr('data-win-control') == "WinJS.UI.AppBar"
                 )
 })(jQuery)
}